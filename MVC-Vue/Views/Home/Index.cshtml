@using MVC_Vue.Controllers
@model HomeDto
﻿@{
    ViewData["Title"] = "Kham pha san pham moi";
}

<v-container class="py-16 text-center">
    <!--Cách render từ Razor: render dựa vào vue. Biến 'clients' được khai báo từ js-->
    @*@await Html.PartialAsync("_HeroSection")*@
    @*@await Html.PartialAsync("_Choose")*@
    <!--Cách render từ Razor: render data sẵn. tốt cho SEO-->
    @*@await Html.PartialAsync("_Carousel", Model.Images)*@
    @await Html.PartialAsync("_Modern", Model.Categories)
</v-container>

<!-- FEATURE CATEGORIES -->
@* <v-container id="home-categories">
    <v-row dense>
        @foreach (var item in Model.Products)
        {
            <v-col cols="12" sm="4" key="@item.Id">
                <v-card class="my-2" href="/Product?id=@item.Id">
                    <v-card-title class="text-h6">@item.Name</v-card-title>
                </v-card>
            </v-col>
        }
    </v-row>
</v-container> *@

@section Scripts {
    <script type="module">
        import { mount } from '/js/site.js';
        mount('#app',
             {
                data() {
                    return {
                        drawer: false,
                    }
                },
                setup() {
                    const tab=Vue.ref(1);
                    return { tab };
                },
                mounted() {
                  console.log("mounted 1");
                },
                methods: {
                    
                }
             },
             //{ 'home-carousel': HomeCarousel }   // đăng ký sớm

        );

    </script>
}

